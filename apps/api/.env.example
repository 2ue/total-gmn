# 数据库连接地址（当前使用 SQLite 本地文件）
DATABASE_URL="file:./dev.db"

# 交易关闭是否按“收/支”方向参与纯收益与分润计算
# true: 参与计算（当前默认推荐）
# false: 不参与计算
PROFIT_INCLUDE_CLOSED_DIRECTION_IN_PROFIT=true

# API 服务监听端口（默认 3001）
PORT=3001

# API 服务监听地址（默认 0.0.0.0）
HOST=0.0.0.0
